def check_data(source) -> {bool, str}:
    """
    Check if there is hash in bytes

    Args:
        source (bytes): bytes to check

    Raises:
        TODO jak coś będzie

    Returns:
        is_hash (bool): True - bytes contain hash, False - bytes don't contain hash
        hash (str): found hash

    """
    is_hash = False
    hash = ""

    # Code goes here...

    return is_hash, hash
